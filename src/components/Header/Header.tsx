import { Box } from "..";

export const Header = () => {
  return (
    <Box
      sx={{
        width: "100%",
        height:"10vh",
        backgroundColor: "red",
      }}
    >
      Header
    </Box>
  );
};
